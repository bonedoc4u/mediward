node_modules/
dist/
.env.local
.env.*.local
*.local

# Capacitor Android - commit the project but not build artifacts
android/.gradle/
android/app/build/
android/build/
.gradle/

.vercel
.env*

# pnpm is the only supported package manager — never commit other lockfiles
package-lock.json
yarn.lock

# Vitest coverage report output
coverage/
