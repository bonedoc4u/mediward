# Hoist workbox-* packages to the top-level node_modules so that sw.ts and
# vite-plugin-pwa virtual modules can import them directly.
# pnpm's strict isolation would otherwise block these transitive deps.
public-hoist-pattern[]=workbox-*